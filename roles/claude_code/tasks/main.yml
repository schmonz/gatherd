---
- name: Merge the Superpowers plugin into Claude Code settings
  ansible.builtin.import_tasks: settings.yml

- name: Install standalone Claude Code skills
  ansible.builtin.import_tasks: skill.yml

- name: Make the doubt-driven-development skill get used, not just installed
  ansible.builtin.import_tasks: doubt.yml
