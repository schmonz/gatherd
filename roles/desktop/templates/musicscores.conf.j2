# Managed by gatherd (roles/desktop/tasks/tablet_mode.yml). Sourced by
# gatherd-musicscores-fetch. forScore on the Mac is the only writer; this
# machine's copy is a read-only mirror, so the fetch only ever pulls.
MUSICSCORES_SOURCE_HOST={{ musicscores_source_host }}
MUSICSCORES_SOURCE_PATH='{{ musicscores_source_path }}'
MUSICSCORES_DEST={{ music_stand_root }}
